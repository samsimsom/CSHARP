<Project Sdk="Microsoft.NET.Sdk">

    <!--
    <PropertyGroup>
        <TargetFramework>net5.0</TargetFramework>
    </PropertyGroup>
    -->
    
    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
    </PropertyGroup>
    
    <!-- 
    To use the lates C# language and .NET platform features, put
    types in a .NET 5 class library. To suppoert lagacy .NET platforms like 
    .NET Core, .NET Framework, and Xamarin, put types that you might reuse in a
    .NET Standart 2.0 class library
    -->

</Project>
